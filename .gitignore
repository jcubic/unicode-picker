node_modules
dist
design
coverage
test-results
playwright-report
*.tgz

# Generated by `npm run gen:data` (reproducible from pinned devDeps; rebuilt via
# the `prepare` script on install and on publish).
providers/data/blocks/
providers/data/emoji.ts
providers/data/index.ts
